# Allocators

This directory contains memory allocator modules shipped with the Swage 
framework. Each subdirectory is a separate crate, containing one or more
allocators implementing the `swage_core::allocator::Allocator` trait.
